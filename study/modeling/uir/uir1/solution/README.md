# УИР 1 — расчётная часть

Параметризованный расчёт: на входе номер варианта, на выходе формы 1–3,
графики 1–3, выбранный закон распределения и готовый markdown-отчёт.

Покрывает **все 360 вариантов** — задание для них одинаковое, отличаются только
данные (столбец из 300 наблюдений).

## Установка и запуск

```bash
python3 -m pip install -r requirements.txt
python3 -m uir1 --variant 49
```

Отчёт появится в `out/variant-049/`: `report.md`, `plots/*.png`, `form1.csv`, `form2.csv`.

### Ключи

| Ключ | Назначение |
|---|---|
| `-v, --variant` | номер варианта, 1…360 (обязательный) |
| `-o, --out` | каталог для результатов (по умолчанию `./out`) |
| `-x, --xlsx` | файл вариантов (по умолчанию из `materials/lms/`) |
| `-l, --law` | принудительный закон: `uniform`, `exponential`, `erlang`, `hypoexponential`, `hyperexponential`; по умолчанию `auto` — выбор по ν |
| `-q` | параметр q гиперэкспоненциального закона |
| `--seed` | seed генератора (по умолчанию фиксирован — прогон воспроизводим) |
| `--list` | сводка по всем 360 вариантам: m̃, ν, выбранный закон |

## Что считается

| Пункт задания | Где |
|---|---|
| Моменты для n = 10/20/50/100/200/300, дов. интервалы 0,9/0,95/0,99 | форма 1 |
| График значений и характер ЧП | график 1 |
| Автокорреляция, сдвиги 1…10 | форма 3 + график |
| Гистограмма частот | график 2 |
| Выбор закона по ν и его параметры | раздел 5 отчёта |
| Алгоритм генератора | раздел 6 отчёта |
| Характеристики сгенерированной ЧП | форма 2 |
| Гистограмма + плотность f(x) | график 3 |
| Корреляция заданной и сгенерированной ЧП | раздел 10 отчёта |

## Модули

| Файл | Ответственность |
|---|---|
| `uir1/variants.py` | чтение xlsx средствами stdlib (zip + XML), без зависимостей |
| `uir1/estimates.py` | оценки моментов, доверительные интервалы, автокорреляция, гистограмма |
| `uir1/approx.py` | пять законов распределения: параметры, плотности, генераторы |
| `uir1/analysis.py` | полный прогон и сборка отчёта с выводами |
| `uir1/report.py` | формы, CSV, графики |

## Выбор закона

| ν | Закон |
|---|---|
| `ν > 1,05` | гиперэкспоненциальный, `q = 1/(1+ν²)` |
| `0,95 ≤ ν ≤ 1,05` | экспоненциальный |
| `ν < 0,95`, `1/ν²` близко к целому | нормированный Эрланга |
| `ν < 0,95`, `1/ν²` далеко от целого, `ν ≥ 0,707` | гипоэкспоненциальный |

Последняя строка — не самодеятельность: задание требует аппроксимации **по двум
моментам**, а округление `k` до целого искажает второй момент. Двухфазная
гипоэкспонента подгоняет оба точно. Когда она недоступна (`ν < 0,707`), берётся
Эрланг, и отчёт печатает, насколько округление исказило `ν`.

Расклад по всем 360 вариантам: гиперэкспоненциальный — 164, Эрланга — 135,
гипоэкспоненциальный — 35, экспоненциальный — 26.

## Тесты

```bash
python3 -m unittest test_uir1 -v
```

23 проверки: несмещённость оценки дисперсии, падение `σ_m` как `1/√n`, сверка
таблицы `t_p` с квантилями нормального распределения, нормировка гистограммы,
воспроизведение моментов каждым генератором, устойчивость на всех 360 вариантах.

## Что нужно доделать руками

Скрипт считает и рисует, но **отчёт за тебя не сдаст**:

- **Характер последовательности** (график 1) определяется автоматически лишь грубо —
  сравнением средних половин. Периодичность и длину периода надо смотреть глазами.
- **Выбор между Эрлангом и равномерным** при `ν < 1/√3` делается по виду гистограммы,
  а не по числу. Отчёт об этом предупреждает, решение за тобой.
- **Титульный лист** и оформление по требованиям кафедры.
- **Демонстрация генератора на защите** — запустить `python3 -m uir1 -v <номер>`
  и показать раздел 6 отчёта.

## Известное ограничение

При `ν < 0,707`, когда `1/ν²` далеко от целого, Эрланг с округлённым `k` искажает `ν` —
в худшем случае по всем вариантам это 9,5 % (вариант 141: `ν = 0,6457`, `k = 2`
даёт `0,7071`). Точная подгонка потребовала бы обобщённого гипоэкспоненциального
закона с разными интенсивностями фаз, которого нет в списке задания. Отчёт печатает
предупреждение с точными числами — это же стоит упомянуть в выводах.
